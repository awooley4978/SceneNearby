// BISECT P1 STUB — replaces real AppNavigator to isolate the production
// import graph. Real file: src/navigation/AppNavigator.tsx (restored in P4).
import React from 'react';
import { View } from 'react-native';

export const AppNavigator = () => <View style={{ flex: 1 }} />;
